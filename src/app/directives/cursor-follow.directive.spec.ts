import { CursorFollowDirective } from './cursor-follow.directive';

describe('CursorFollowDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorFollowDirective();
    expect(directive).toBeTruthy();
  });
});
