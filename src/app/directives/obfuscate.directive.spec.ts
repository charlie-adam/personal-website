import { ObfuscateDirective } from './obfuscate.directive';

describe('ObfuscateDirective', () => {
  it('should create an instance', () => {
    const directive = new ObfuscateDirective();
    expect(directive).toBeTruthy();
  });
});
